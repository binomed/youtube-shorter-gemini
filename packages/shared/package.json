{
    "name": "@youtube-shorter/shared",
    "version": "1.0.0",
    "private": true,
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc -w",
        "lint": "eslint src --ext .ts",
        "test": "jest"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "jest": "^30.0.0",
        "ts-jest": "^29.2.5",
        "typescript": "latest"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node",
        "roots": [
            "<rootDir>/test"
        ],
        "testMatch": [
            "**/*.spec.ts"
        ]
    },
    "dependencies": {
        "class-transformer": "^0.5.1",
        "class-validator": "^0.14.3"
    }
}
